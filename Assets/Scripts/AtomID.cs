using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Component of the atom prefab, so of each instance of an atom
public class AtomID : MonoBehaviour {
    // stores the unique ID each atom gets when it is created
    public int ID;
}
